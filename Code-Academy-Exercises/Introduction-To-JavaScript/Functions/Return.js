function monitorCount(rows, columns) {
  return rows * columns;
}

const numOfMonitors = monitorCount(5,4);

console.log(numOfMonitors);

//I used a return command to give the value of the rows times the columns from the function.  I assigned the return value to a const
