const logVisibleLightWaves = () => {
  const lightWaves = 'Moonlight';
  console.log(lightWaves);
};

logVisibleLightWaves();
console.log(lightWaves);

//I used a variable inside my function and couldn't access it outside of the function because it is a local variable
