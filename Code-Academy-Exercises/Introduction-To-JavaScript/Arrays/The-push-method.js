const chores = ['wash dishes', 'do laundry', 'take out trash'];

chores.push('sweep the floor', 'clean the tub');

console.log(chores);

// I added two items to the end of the array with the push method
